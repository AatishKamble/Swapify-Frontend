

import {CREATE_ADDRESS_REQUEST,
    CREATE_ADDRESS_SUCCESS,
    CREATE_ADDRESS_FAILURE,GET_ADDRESS_REQUEST,
    GET_ADDRESS_SUCCESS,GET_ADDRESS_FAILURE} from "./ActionType.js";

    function createNewAddress(reqData){
dispatchEvent({type:CREATE_ADDRESS_REQUEST});
        try {
            
        } catch (error) {
            
        }
    }